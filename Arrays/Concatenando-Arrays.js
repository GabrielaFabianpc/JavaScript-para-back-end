const salaJs = ["Gabi", "Trajano", "Elisa"];
const salaPython = ["Daniele", "Nicolas", "Keren"];

const salasUnificadas = salaJs.concat(salaPython); // criamos uma nova const pois vai criar um novo array.
console.log(salasUnificadas);
